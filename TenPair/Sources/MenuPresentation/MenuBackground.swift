import SwiftUI
import UIComponents

internal struct MenuBackground: UIViewRepresentable {
    func makeUIView(context: Context) -> some UIView {
        OverlayBackgroundView()
    }
    
    func updateUIView(_ uiView: UIViewType, context: Context) {
        
    }
}

